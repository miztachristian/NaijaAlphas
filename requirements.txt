# NGX Stock Analysis System - Requirements
# =========================================

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0

# Data acquisition (TradingView via Playwright)
# playwright is installed separately: pip install playwright && playwright install chromium

# Data storage (optional but recommended)
pyarrow>=8.0.0  # For parquet file support

# Configuration
pyyaml>=6.0.0

# Progress display (optional)
tqdm>=4.62.0

# Visualization (optional)
matplotlib>=3.4.0
seaborn>=0.11.0
plotly>=5.0.0

# Jupyter support (optional)
jupyter>=1.0.0
ipykernel>=6.0.0

# News scraping & sentiment analysis
feedparser>=6.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
cloudscraper>=1.2.71  # Cloudflare bypass for AfricanFinancials
pypdf>=4.0.0          # PDF text extraction for AFF annual reports

# Testing
pytest>=7.0.0

# Telegram notification layer + local AI agent
python-dotenv>=1.0.0
python-telegram-bot>=21.0
ollama>=0.3.0
